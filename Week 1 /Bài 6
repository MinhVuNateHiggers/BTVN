#include <bits/stdc++.h>
#define int long long
using namespace std;
int n;
int a[1111];
int32_t main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
    }
    for (int i = n; i >= 1; i--)
    {
        cout << a[i] << " ";
    }
}
